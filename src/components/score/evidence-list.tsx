export function EvidenceList({ observations }: { observations: Array<{ id: string; rawResponse: string; platformId: string; promptVersion: { text: string }; mentions: Array<{ isTarget: boolean; evidence: string }>; citations: Array<{ url: string; domain: string }> }> }) {
  return <div className="evidence-list">{observations.map((observation) => <details key={observation.id}><summary><div><span>{observation.platformId}</span><strong>{observation.promptVersion.text}</strong></div><b>{observation.mentions.some((item) => item.isTarget) ? "已提及" : "未提及"}</b></summary><div className="evidence-body"><p>{observation.rawResponse}</p>{observation.citations.length > 0 && <ul>{observation.citations.map((citation) => <li key={citation.url}><a href={citation.url} target="_blank" rel="noreferrer">{citation.domain}</a></li>)}</ul>}</div></details>)}</div>;
}
